<!DOCTYPE html>
<html lang="en">
  <head>
    <title>New Volunteer Information</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="styles/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Georama:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Archivo:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
  </head>
 <body class="min-vh-100 d-flex justify-content-center align-items-center bg-body-tertiary">
  <main class="container body-container">
    <div class="row justify-content-center">
      <div class="col-12 col-md-10 col-lg-8 form-entry">
        <!-- Banner/Card replacement for jumbotron -->
        <section id="contact-info" class="card shadow-lg">
          <header class="card-header text-center">
        <h1 class="text-center mb-4">Contact Information</h1>
          <form id="phoneVer-form" action="/submit-phoneVer" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <!-- Name Fields -->
            <div class="mb-3">
              <label for="firstName" class="form-label">First Name:<span class="asterix">*</span></label>
              <input type="text" id="firstName" name="firstName" class="form-control" <%= disableNameFields ? 'disabled': '' %> <%= disableNameFields ? '' : 'required' %> <%= disableNameFields ? '' : 'visible' %>>
              <label for="lastName" class="form-label">Last Name:<span class="asterix">*</span></label>
              <input type="text" id="lastName" name="lastName" class="form-control" <%= disableNameFields ? 'disabled': '' %> <%= disableNameFields ? '' : 'required' %> <%= disableNameFields ? '' : 'visible' %>>

              <label for="suffix" class="form-label">Suffix (if any):</label>
              <input type="text" id="suffix" name="suffix" class="form-control">
            </div>
            <!-- Cell Phone Field -->
            <div class="mb-3">
              <label for="phone" class="form-label">Phone:<span class="asterix">*</span></label>
              <input type="text" id="phone" name="phone" class="form-control" placeholder="(xxx) xxx-xxxx" inputmode="numeric" aria-describedby="phone-status" required>
              <label for="confirm-phone" class="form-label mt-3">Confirm Phone:<span class="asterix">*</span></label>
              <input type="text" id="confirm-phone" name="confirm-phone" class="form-control" disabled placeholder="(xxx) xxx-xxxx" inputmode="numeric" aria-describedby="confirm-phone-status" required>
              <div id="phone-status" class="status mt-2" role="status" aria-live="polite"></div>
              <div id="confirm-phone-status" class="status mt-2" role="status" aria-live="polite"></div>
              <!-- SMSCapable radio group -->
              <fieldset class="border-0 p-0 mt-3" aria-describedby="SMSCapable-help" aria-labelledby="SMSCapable-legend">
                <legend id="SMSCapable-legend" class="form-label mb-2">
                  Can you receive text messages at this number?<span class="asterix">*</span>
                </legend>
                <div id="SMSCapable-help" class="form-text mb-2">
                  Select <strong>Yes</strong> if your device can receive text messages.
                </div>
                <div class="d-flex flex-wrap align-items-center gap-4" role="radiogroup" aria-labelledby="SMSCapable-legend" aria-describedby="SMSCapable-help">
                  <div class="form-check">
                    <input class="form-check-input" type="radio" name="SMSCapable" id="SMSCapable-yes" value="yes" required>
                    <label class="form-check-label" for="SMSCapable-yes">Yes</label>
                  </div>
                  <div class="form-check">
                    <input class="form-check-input" type="radio" name="SMSCapable" id="SMSCapable-no" value="no">
                    <label class="form-check-label" for="SMSCapable-no">No</label>
                  </div>
                </div>
                <div class="invalid-feedback" id="SMSCapable-error" aria-live="polite">
                  Please select Yes or No.
                </div>
              </fieldset>
            </div>
            <!-- Submit Button -->
            <!-- <div class="row row-cols-1 row-cols-md-2 align-items-md-center"> -->
            <div id="submit-status" class="status mt-2" role="status" aria-live="polite"></div>
            <button type="submit" class="btn btn-primary" id="submit-btn" disabled>Submit</button>
              <p class="footnote"><span class="asterix">*</span> Indicates required fields</p>

          </form>
      </section>
  </main>


  <!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/phoneVer.js" ></script>

  <!-- Custom JS -->
</body>
</html>